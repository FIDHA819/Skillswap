
export enum Role {
  STUDENT = "student",
  ADMIN = "admin",
  TEACHER = "teacher",
}

export enum UserStatus {
  PENDING = "pending",
  ACTIVE = "active",
  BLOCKED = "blocked"
}

export enum KycStatus {
  PENDING = "pending",
  VERIFIED = "verified",
  REJECTED = "rejected",
}
